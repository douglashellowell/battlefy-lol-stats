import { Match } from '../../types';
import styles from './MatchList.module.scss';

type MatchListProps = {
  matches: Match[];
};

const MatchList = () => {
  return <div></div>;
};

export default MatchList;
